# roter
A Safari extension to rotate your browser tabs

## Tests

- `npm run test:unit`: run the JavaScript helper tests.
- `npm run test:e2e:docker`: build and run the Dockerized Playwright E2E test.
